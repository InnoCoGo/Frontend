export type dateInfo = { date: string, dateElement: HTMLElement }
export type telegramInfo = {
    id: number,
    first_name: string,
    last_name: string,
    username: string,
    photo_url: string,
    auth_date: number,
    hash: string
}